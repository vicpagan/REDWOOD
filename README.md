# REDWOOD

## Usage
``` make ```
``` ./main <t> <d> <delta multplier>```
Where `t` is execution time of the task, 
`d` is the deadline for the task, 
and `delta multiplier` is a factor to scale the delta value for testing.

## Example
``` ./main 1000 5000 2```
